<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="Theme.AppCompat.Light.NoActionBar.AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- toolbar（actionbar）颜色 -->
        <item name="colorPrimary">@color/titleBackground</item>
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">@color/title_background_dark</item>
        <!-- 窗口的背景颜色 -->
        <item name="android:windowBackground">@color/main_gray_background</item>
        <!-- 获取焦点的颜色 -->
        <item name="colorAccent">@color/title_background_dark</item>
    </style>

    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar.AppBaseTheme"/>

    <!--TabLayout字号设置-->
    <style name="TabLayoutTextStyle">
        <item name="android:textSize">@dimen/select_top_text_size</item>
        <item name="android:layout_margin">@dimen/default_margin</item>
    </style>

    <!--cardView常用配置-->
    <style name="MyBaseCard" parent="@style/CardView.Light">
        <item name="cardPreventCornerOverlap">false</item>
        <item name="cardUseCompatPadding">false</item>
        <item name="cardElevation">20dp</item>
        <item name="cardCornerRadius">15dp</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
    </style>
</resources>